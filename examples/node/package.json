{
  "name": "node_rust",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "runWasm": "node dist/wasmRules.js",
    "purge": "rm -rf node_modules && rm -rf dist && rm -rf wasm_rules/pkg && rm -rf wasm_rules/target"
  },
  "dependencies": {
    "wasm-rules": "file:wasm_rules/pkg"
  },
  "devDependencies": {
    "@types/node": "^17.0.35",
    "typescript": "^4.6.4"
  },
  "license": "MIT"
}
